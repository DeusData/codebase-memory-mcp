---
phase: 04-verdicts-and-acceptance-summaries
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - docs/superpowers/proofs/gdscript-real-project-validation.md
  - docs/superpowers/proofs/gdscript-good-tier-checklist.md
  - docs/superpowers/proofs/gdscript-good-tier-misses.md
autonomous: true
requirements:
  - PROOF-03
  - EVID-03
must_haves:
  truths:
    - "Maintainers can read the runbook and know how to interpret repo verdicts and the promotion decision block."
    - "The checklist uses the same promotion-safe wording as the generated summaries."
    - "The misses tracker tells maintainers how to record fail versus incomplete blocking evidence after each run."
  artifacts:
    - path: "docs/superpowers/proofs/gdscript-real-project-validation.md"
      provides: "Runbook contract for verdict-first repo review and promotion-first aggregate review"
      contains: "## Promotion decision"
    - path: "docs/superpowers/proofs/gdscript-good-tier-checklist.md"
      provides: "Operator checklist aligned to qualified-support-only vs do-not-promote wording"
      contains: "qualified-support-only"
    - path: "docs/superpowers/proofs/gdscript-good-tier-misses.md"
      provides: "Miss-tracking instructions that distinguish fail and incomplete blocking states"
      contains: "Current Outcome"
  key_links:
    - from: "docs/superpowers/proofs/gdscript-real-project-validation.md"
      to: "scripts/gdscript-proof.sh"
      via: "documented summary contract"
      pattern: "Promotion answer:"
    - from: "docs/superpowers/proofs/gdscript-good-tier-checklist.md"
      to: "aggregate-summary.md"
      via: "operator review gate"
      pattern: "do-not-promote"
---

<objective>
Document the Phase 4 verdict contract so maintainers review proof output consistently and keep promotion claims honest after each manifest run.

Purpose: Make the new summary behavior durable in docs and checklists instead of leaving it as script-only knowledge.
Output: Updated runbook, promotion checklist, and misses-tracker guidance aligned with D-01 through D-05.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verdicts-and-acceptance-summaries/04-CONTEXT.md
@docs/superpowers/proofs/gdscript-real-project-validation.md
@docs/superpowers/proofs/gdscript-good-tier-checklist.md
@docs/superpowers/proofs/gdscript-good-tier-misses.md
@scripts/gdscript-proof.sh

<interfaces>
Document the exact summary contract introduced by Plan 01:

```text
## Verdict
- Repo verdict: `pass|fail|incomplete`
- Approval contribution: `counts-toward-qualified-support` | `does-not-count-toward-promotion`

## Promotion decision
- Promotion answer: `qualified-support-only` | `do-not-promote`
- Claim scope: approved manifest corpus only; current commit only
- Promotion rationale: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: update the runbook and checklist for verdict-first review</name>
  <files>docs/superpowers/proofs/gdscript-real-project-validation.md, docs/superpowers/proofs/gdscript-good-tier-checklist.md</files>
  <read_first>
    - docs/superpowers/proofs/gdscript-real-project-validation.md
    - docs/superpowers/proofs/gdscript-good-tier-checklist.md
    - scripts/gdscript-proof.sh
    - .planning/phases/04-verdicts-and-acceptance-summaries/04-CONTEXT.md
  </read_first>
  <action>Update the runbook and checklist to match the new executable summary contract from Plan 01. Per D-01 and D-02, change the aggregate-summary guidance so maintainers review `## Promotion decision` before totals or repo tables. Per D-03 through D-05, document that both `fail` and `incomplete` require `Promotion answer: do-not-promote`, while aggregate `pass` only supports `Promotion answer: qualified-support-only` with the explicit scope `approved manifest corpus only; current commit only`. Update the per-repo summary section to call out `## Verdict`, `Repo verdict`, and `Approval contribution`. Update the operator checklist so it explicitly checks for `qualified-support-only` on pass and `do-not-promote` on fail/incomplete rather than vague promotion language.</action>
  <acceptance_criteria>
    - `docs/superpowers/proofs/gdscript-real-project-validation.md` contains `## Promotion decision`, `Promotion answer: qualified-support-only`, `Promotion answer: do-not-promote`, and `Claim scope: approved manifest corpus only; current commit only`.
    - `docs/superpowers/proofs/gdscript-real-project-validation.md` contains `## Verdict`, `Repo verdict:`, and `Approval contribution:` in the per-repo summary guidance.
    - `docs/superpowers/proofs/gdscript-good-tier-checklist.md` contains both `qualified-support-only` and `do-not-promote` as review gates.
  </acceptance_criteria>
  <verify>
    <automated>python3 -B scripts/test_gdscript_proof_verdict_summaries.py && python3 -B scripts/test_gdscript_proof_manifest_contract.py</automated>
  </verify>
  <done>The human-facing runbook and checklist now describe the same verdict and promotion contract that the harness emits.</done>
</task>

<task type="auto">
  <name>task 2: align the proof-misses tracker with fail vs incomplete blocking states</name>
  <files>docs/superpowers/proofs/gdscript-good-tier-misses.md</files>
  <read_first>
    - docs/superpowers/proofs/gdscript-good-tier-misses.md
    - docs/superpowers/proofs/gdscript-real-project-validation.md
    - .planning/phases/04-verdicts-and-acceptance-summaries/04-CONTEXT.md
  </read_first>
  <action>Revise `docs/superpowers/proofs/gdscript-good-tier-misses.md` so it matches the Phase 4 summary contract. Keep one row per failing or incomplete gating assertion, but add explicit guidance that `fail` means comparable evidence disproved the gate while `incomplete` means promotion is blocked because evidence is missing or non-comparable per D-03. Add instructions to capture the repo summary verdict, aggregate promotion answer, and the artifact path(s) maintainers should inspect next (`repo-meta.json`, `run-index.json`, or wrapper `queries/*.json`). Do not turn advisory notes into promotion approval language.</action>
  <acceptance_criteria>
    - `docs/superpowers/proofs/gdscript-good-tier-misses.md` explains the difference between `fail` and `incomplete` for promotion blocking.
    - `docs/superpowers/proofs/gdscript-good-tier-misses.md` instructs maintainers to record the aggregate promotion answer and follow-up artifact paths.
    - `docs/superpowers/proofs/gdscript-good-tier-misses.md` still preserves separate sections for open gating misses, advisory mismatches, and closed misses.
  </acceptance_criteria>
  <verify>
    <automated>python3 -B scripts/test_gdscript_proof_verdict_summaries.py && python3 -B scripts/test_gdscript_proof_incomplete_artifacts.py</automated>
  </verify>
  <done>The maintenance docs now preserve the semantic difference between fail and incomplete while keeping both states promotion-blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated proof summaries → maintainer documentation | Human operators rely on docs/checklists to interpret untrusted proof output correctly |
| misses tracker updates → future promotion decisions | Recorded misses can influence whether support claims are broadened later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | I | runbook / checklist wording | mitigate | Mirror the exact `qualified-support-only` and `do-not-promote` strings from generated summaries so docs cannot imply broader support than the harness proves. |
| T-04-05 | R | `gdscript-good-tier-misses.md` | mitigate | Require misses tracking to record fail vs incomplete plus artifact follow-ups so future promotion decisions remain auditable. |
| T-04-06 | T | operator review flow | mitigate | Keep wrapper-file and run-index references in the docs so maintainers verify verdicts against underlying evidence instead of editing summaries by hand. |
</threat_model>

<verification>
Re-run the verdict-summary regression after doc changes to confirm the documented contract still matches the harness output.
</verification>

<success_criteria>
- The runbook tells maintainers to read promotion status first.
- The checklist uses the same promotion-safe vocabulary as the harness.
- The misses tracker distinguishes comparable failures from incomplete evidence while treating both as blocking states.
</success_criteria>

<output>
After completion, create `.planning/phases/04-verdicts-and-acceptance-summaries/04-verdicts-and-acceptance-summaries-02-SUMMARY.md`
</output>
