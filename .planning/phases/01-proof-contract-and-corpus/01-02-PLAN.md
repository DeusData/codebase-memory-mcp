---
phase: 01-proof-contract-and-corpus
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - scripts/gdscript-proof.sh
  - scripts/test_gdscript_proof_manifest_contract.py
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - Approval-bearing proof runs enforce the committed manifest corpus instead of silently accepting ad hoc target identity.
    - Repo metadata and summaries expose enough identity information to rerun the same target and determine whether it qualifies for v1.
    - Ad hoc runs remain usable for debugging but are clearly marked as non-canonical and non-qualifying.
  artifacts:
    - path: scripts/gdscript-proof.sh
      provides: Proof-harness enforcement of corpus and identity contract
    - path: scripts/test_gdscript_proof_manifest_contract.py
      provides: Regression coverage for manifest identity and ad hoc lane guardrails
  key_links:
    - from: scripts/gdscript-proof.sh
      to: docs/superpowers/proofs/gdscript-good-tier-manifest.json
      via: Manifest parsing and validation
      pattern: manifest
    - from: scripts/test_gdscript_proof_manifest_contract.py
      to: scripts/gdscript-proof.sh
      via: Subprocess proof run assertions
      pattern: gdscript-proof.sh
---

<objective>
Make the Phase 1 proof contract executable by tightening the proof harness and adding regression coverage for approval-bearing versus ad hoc behavior.

Purpose: Ensure the corpus and identity rules are not just documented; they are enforced in machine-readable proof output.
Output: Updated harness behavior plus a focused regression test for the manifest contract.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-proof-contract-and-corpus/01-CONTEXT.md
@.planning/phases/01-proof-contract-and-corpus/01-RESEARCH.md
@.planning/phases/01-proof-contract-and-corpus/01-01-PLAN.md
@docs/superpowers/proofs/gdscript-good-tier-manifest.json
@scripts/gdscript-proof.sh
@scripts/test_gdscript_proof_same_script_calls.py

<interfaces>
From `scripts/gdscript-proof.sh`:

```bash
parse_args()
prepare_repo_metadata()
write_repo_meta_json()
write_repo_and_aggregate_summaries()
apply_manifest_defaults()
manifest_repo_godot_version()
```

Executor should extend the existing manifest/metadata path rather than add a parallel contract checker.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: enforce canonical approval metadata in proof output</name>
  <files>scripts/gdscript-proof.sh</files>
  <read_first>
    scripts/gdscript-proof.sh
    docs/superpowers/proofs/gdscript-good-tier-manifest.json
    docs/superpowers/proofs/gdscript-real-project-validation.md
    .planning/phases/01-proof-contract-and-corpus/01-CONTEXT.md
  </read_first>
  <behavior>
    - Manifest-mode output records canonical identity using `remote`, `pinned_commit`, `project_subpath` when present, and recorded `godot_version` per D-04 and D-05.
    - Labels remain readability metadata, and local checkout path remains run evidence only per D-06 and D-07.
    - Ad hoc `--repo` runs are explicitly marked non-canonical and non-qualifying per D-09 and D-11.
  </behavior>
  <action>Modify `scripts/gdscript-proof.sh` so manifest-driven runs validate and emit the approval contract consistently in `repo-meta.json`, repo summaries, and aggregate summary text. Extend the current metadata-preparation and summary-writing helpers instead of introducing a new script. Add explicit fields or summary lines for canonical approval status, qualifying/non-qualifying Godot status, and canonical identity components using the exact tuple from D-04. When a target is not in manifest mode or does not have confirmed Godot 4.x metadata, preserve run execution but label the target non-canonical/non-qualifying rather than letting it look approval-bearing. Keep existing manifest-driven proof behavior and existing same-script-call evidence logic intact.</action>
  <acceptance_criteria>
    - `scripts/gdscript-proof.sh` writes machine-readable metadata that distinguishes canonical approval-bearing targets from ad hoc/debug targets.
    - Summary generation includes explicit qualifying/non-qualifying language rather than relying on implied interpretation.
    - The script still supports ad hoc `--repo` execution without requiring manifest mode.
    - Existing same-script-call proof behavior is not removed.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/test_gdscript_proof_same_script_calls.py</automated>
  </verify>
  <done>Proof artifacts themselves show which targets count for approval and why, without breaking current proof runs.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add a regression for manifest identity and debug-lane guardrails</name>
  <files>scripts/test_gdscript_proof_manifest_contract.py</files>
  <read_first>
    scripts/test_gdscript_proof_same_script_calls.py
    scripts/gdscript-proof.sh
    docs/superpowers/proofs/gdscript-good-tier-manifest.json
    .planning/phases/01-proof-contract-and-corpus/01-CONTEXT.md
  </read_first>
  <behavior>
    - A shared-remote proof target with `project_subpath` remains distinguishable in machine-readable output.
    - A non-manifest/ad hoc run completes but is labeled non-canonical and non-qualifying.
  </behavior>
  <action>Create `scripts/test_gdscript_proof_manifest_contract.py` following the subprocess/temp-repo pattern in `scripts/test_gdscript_proof_same_script_calls.py`. The test must build temporary fixture repos and/or fixture manifests as needed, run `scripts/gdscript-proof.sh`, read `repo-meta.json`, `summary.md`, and `aggregate-summary.md`, and assert the new contract behavior from task 1. At minimum, assert that approval-bearing manifest output includes canonical identity fields and that an ad hoc run is explicitly marked as non-canonical/non-qualifying instead of looking like accepted corpus evidence.</action>
  <acceptance_criteria>
    - `scripts/test_gdscript_proof_manifest_contract.py` exists and exits 0 when the contract is satisfied.
    - The test reads generated proof artifacts rather than only checking process exit code.
    - The test asserts both manifest-mode canonical identity behavior and ad hoc non-canonical behavior.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/test_gdscript_proof_manifest_contract.py</automated>
  </verify>
  <done>A failing regression would catch future drift in canonical identity or approval-lane labeling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest input → proof harness | Untrusted manifest metadata is converted into approval-bearing proof output |
| proof harness → summary/metadata artifacts | Operators trust generated files to decide whether a run counted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `scripts/gdscript-proof.sh` manifest handling | mitigate | Validate and emit canonical identity fields directly from manifest-mode inputs so target identity cannot silently collapse to label/path-only metadata. |
| T-01-05 | R | generated `repo-meta.json` and summaries | mitigate | Persist explicit canonical/non-canonical and qualifying/non-qualifying markers in machine-readable and Markdown outputs. |
| T-01-06 | D | ad hoc debug lane | accept | Keep ad hoc runs available for investigation, but mark them non-canonical instead of blocking execution; local-maintainer-only workflow makes this low risk. |
</threat_model>

<verification>
- Run both proof regression scripts and confirm they pass.
- Inspect one generated `repo-meta.json` from the new regression to confirm canonical identity fields are present.
- Inspect one ad hoc-run summary from the new regression to confirm it is labeled non-canonical/non-qualifying.
</verification>

<success_criteria>
- Proof output itself distinguishes approved corpus targets from debug-only runs.
- Shared-upstream targets remain rerunnable because `project_subpath` and other identity fields are preserved.
- Regression coverage exists for both manifest identity and ad hoc guardrails.
</success_criteria>

<output>
After completion, create `.planning/phases/01-proof-contract-and-corpus/01-proof-contract-and-corpus-02-SUMMARY.md`
</output>
