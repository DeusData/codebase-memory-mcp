---
phase: 01-proof-contract-and-corpus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/superpowers/proofs/gdscript-good-tier-manifest.json
  - docs/superpowers/proofs/gdscript-real-project-validation.md
  - docs/superpowers/proofs/gdscript-good-tier-checklist.md
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - Maintainer can identify the approved v1 proof corpus from the committed manifest instead of choosing arbitrary repos at runtime.
    - Each approved proof target exposes canonical identity fields that are sufficient to rerun the same upstream project again.
    - Only targets with explicitly recorded Godot 4.x qualification count toward v1 approval.
    - Ad hoc proof runs remain available for debugging but are clearly documented as non-canonical.
  artifacts:
    - path: docs/superpowers/proofs/gdscript-good-tier-manifest.json
      provides: Approved proof corpus with pinned identity metadata
    - path: docs/superpowers/proofs/gdscript-real-project-validation.md
      provides: Operator contract for manifest and ad hoc lanes
    - path: docs/superpowers/proofs/gdscript-good-tier-checklist.md
      provides: Human verification checklist aligned to the contract
  key_links:
    - from: docs/superpowers/proofs/gdscript-real-project-validation.md
      to: docs/superpowers/proofs/gdscript-good-tier-manifest.json
      via: Manifest-mode instructions and examples
      pattern: manifest
    - from: docs/superpowers/proofs/gdscript-good-tier-checklist.md
      to: docs/superpowers/proofs/gdscript-good-tier-manifest.json
      via: Approval gate language
      pattern: Godot 4.x|pinned
---

<objective>
Lock the written Phase 1 proof contract around the committed manifest so maintainers know exactly which targets are approved and why.

Purpose: Make approval-bearing proof selection deterministic before enforcement logic is tightened.
Output: An explicit manifest+runbook+checklist contract for the approved Godot 4.x proof corpus.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-proof-contract-and-corpus/01-CONTEXT.md
@.planning/phases/01-proof-contract-and-corpus/01-RESEARCH.md
@docs/superpowers/proofs/gdscript-good-tier-manifest.json
@docs/superpowers/proofs/gdscript-real-project-validation.md
@docs/superpowers/proofs/gdscript-good-tier-checklist.md
@docs/superpowers/specs/2026-04-04-gdscript-real-project-proof-design.md
</context>

<tasks>

<task type="auto">
  <name>task 1: make the approved corpus contract explicit</name>
  <files>docs/superpowers/proofs/gdscript-good-tier-manifest.json</files>
  <read_first>
    docs/superpowers/proofs/gdscript-good-tier-manifest.json
    .planning/phases/01-proof-contract-and-corpus/01-CONTEXT.md
    docs/superpowers/specs/2026-04-04-gdscript-real-project-proof-design.md
  </read_first>
  <action>Update `docs/superpowers/proofs/gdscript-good-tier-manifest.json` so the committed four-target corpus remains the only approval-bearing v1 set per D-01, D-02, D-03, and D-10. Preserve the same four labels already present. Add or normalize manifest-level and per-target fields needed to make the contract unambiguous: keep `remote`, `pinned_commit`, `project_subpath` when applicable, and recorded `godot_version`; add explicit machine-readable qualification/canonicality metadata if it is not already present so approved targets are visibly confirmed Godot 4.x per D-08 and ad hoc/non-qualifying cases can be represented without ambiguity per D-09 and D-11. Do not add new repos, do not allow "any 3 qualifying repos" logic, and do not treat `label` or local checkout path as canonical identity per D-04 through D-07.</action>
  <acceptance_criteria>
    - `docs/superpowers/proofs/gdscript-good-tier-manifest.json` still contains exactly 4 repo entries.
    - Each repo entry contains `remote`, `pinned_commit`, and `godot_version`.
    - Shared-upstream entries for `squash-the-creeps`, `webrtc-signaling`, and `webrtc-minimal` include `project_subpath`.
    - Manifest text contains explicit qualifying/canonical metadata for approval-bearing entries rather than relying on implied status.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json, pathlib; p=pathlib.Path('docs/superpowers/proofs/gdscript-good-tier-manifest.json'); data=json.loads(p.read_text()); assert len(data['repos'])==4; labels={r['label'] for r in data['repos']}; assert labels=={'squash-the-creeps','webrtc-signaling','webrtc-minimal','topdown-starter'}; [r['remote'] for r in data['repos']]; [r['pinned_commit'] for r in data['repos']]; [r['godot_version'] for r in data['repos']]; shared={r['label']:r.get('project_subpath') for r in data['repos']}; assert shared['squash-the-creeps']; assert shared['webrtc-signaling']; assert shared['webrtc-minimal']"</automated>
  </verify>
  <done>The manifest alone is enough to identify the approved corpus and each approved target's canonical identity contract.</done>
</task>

<task type="auto">
  <name>task 2: align the operator docs with the locked contract</name>
  <files>docs/superpowers/proofs/gdscript-real-project-validation.md, docs/superpowers/proofs/gdscript-good-tier-checklist.md</files>
  <read_first>
    docs/superpowers/proofs/gdscript-real-project-validation.md
    docs/superpowers/proofs/gdscript-good-tier-checklist.md
    docs/superpowers/proofs/gdscript-good-tier-manifest.json
    .planning/phases/01-proof-contract-and-corpus/01-CONTEXT.md
  </read_first>
  <action>Update the runbook and checklist so they state the exact approval contract in prose and operator guidance. The runbook must explicitly say manifest mode is the only approval-bearing lane per D-10, ad hoc `--repo` runs are debugging/investigation only per D-11, canonical target identity is `remote + pinned_commit + project_subpath (when needed) + recorded godot_version` per D-04 and D-05, labels are readability metadata per D-06, and local checkout path is run evidence only per D-07. The checklist must verify that the approved corpus is manifest-defined, pinned, and explicitly Godot 4.x qualified per D-08 instead of assumed.</action>
  <acceptance_criteria>
    - `gdscript-real-project-validation.md` contains language stating manifest mode is the only approval-bearing workflow.
    - `gdscript-real-project-validation.md` contains the canonical identity tuple and states that labels are non-canonical metadata.
    - `gdscript-real-project-validation.md` states ad hoc `--repo` runs are non-canonical debugging/investigation lanes.
    - `gdscript-good-tier-checklist.md` includes an explicit Godot 4.x qualification gate for approved targets.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from pathlib import Path; runbook=Path('docs/superpowers/proofs/gdscript-real-project-validation.md').read_text(); checklist=Path('docs/superpowers/proofs/gdscript-good-tier-checklist.md').read_text(); assert 'approval-bearing' in runbook; assert 'manifest mode' in runbook.lower(); assert 'remote' in runbook and 'pinned_commit' in runbook and 'project_subpath' in runbook and 'godot_version' in runbook; assert 'label' in runbook and 'non-canonical' in runbook; assert '--repo' in runbook and 'debug' in runbook.lower(); assert 'Godot 4.x' in checklist"</automated>
  </verify>
  <done>Maintainers reading the docs can distinguish approved corpus runs from debugging lanes and can see exactly what makes a target canonical and qualifying.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → manifest/docs | A local maintainer edits the approval contract that later proof runs will trust |
| manifest contract → proof operator | Operators rely on committed docs and manifest fields to decide which targets count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `gdscript-good-tier-manifest.json` | mitigate | Make the approved corpus explicit and pinned; require committed manifest changes for corpus updates rather than runtime repo selection. |
| T-01-02 | R | `gdscript-real-project-validation.md` | mitigate | Document one approval-bearing path and canonical identity tuple so operators can audit why a target counted. |
| T-01-03 | S | ad hoc proof invocation guidance | accept | Local maintainers can still run ad hoc debug lanes, but docs must label them non-canonical and non-qualifying so they cannot masquerade as approved evidence. |
|
</threat_model>

<verification>
- Re-run the task-level automated checks.
- Read the manifest and runbook together to confirm they describe the same approved four-target corpus and identity tuple.
- Confirm no deferred items (query suite, parity evidence, verdict summaries) were added to this plan.
</verification>

<success_criteria>
- The committed manifest is the single explicit source of approved v1 proof targets.
- Canonical identity and Godot 4.x qualification are documented without ambiguity.
- Operators can tell which runs count for acceptance and which are debug-only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-proof-contract-and-corpus/01-proof-contract-and-corpus-01-SUMMARY.md`
</output>
