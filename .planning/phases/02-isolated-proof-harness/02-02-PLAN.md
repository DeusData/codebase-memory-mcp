---
phase: 02-isolated-proof-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - docs/superpowers/proofs/gdscript-real-project-validation.md
  - scripts/test_gdscript_proof_manifest_contract.py
  - scripts/test_gdscript_proof_incomplete_artifacts.py
autonomous: true
requirements:
  - EVID-01
  - EVID-02
must_haves:
  truths:
    - "Maintainer can read the runbook and know where isolated state, raw query wrappers, and the run-level index live."
    - "A regression proves partial proof runs keep inspectable artifacts instead of disappearing behind a non-zero exit code."
    - "The documented raw evidence contract matches what the harness actually writes."
  artifacts:
    - path: "docs/superpowers/proofs/gdscript-real-project-validation.md"
      provides: "Operator contract for run index, wrapper-first evidence, and incomplete-path inspection"
      contains: "run-index.json"
    - path: "scripts/test_gdscript_proof_incomplete_artifacts.py"
      provides: "Regression for incomplete-path artifact preservation"
      contains: "Proof run root"
  key_links:
    - from: "scripts/test_gdscript_proof_incomplete_artifacts.py"
      to: "scripts/gdscript-proof.sh"
      via: "subprocess.run"
      pattern: "gdscript-proof.sh"
    - from: "docs/superpowers/proofs/gdscript-real-project-validation.md"
      to: "scripts/gdscript-proof.sh"
      via: "documented artifact layout"
      pattern: "run-index.json"
---

<objective>
Document the Phase 2 evidence contract and lock it in with regression coverage for both success and incomplete-proof paths.

Purpose: Make the new harness behavior usable and durable so maintainers and later phases can rely on the machine-readable artifact layout.
Output: Updated runbook plus regression coverage for isolated-state artifact layout and incomplete-run preservation.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-isolated-proof-harness/02-CONTEXT.md
@.planning/phases/02-isolated-proof-harness/02-RESEARCH.md
@docs/superpowers/proofs/gdscript-real-project-validation.md
@scripts/gdscript-proof.sh
@scripts/test_gdscript_proof_manifest_contract.py
@scripts/test_gdscript_proof_same_script_calls.py

<interfaces>
Expected artifact family after Plan 02-01:

```text
.artifacts/gdscript-proof/<run>/
  run-index.json
  aggregate-summary.md
  <slug>/repo-meta.json
  <slug>/summary.md
  <slug>/queries/*.json
  state/<slug>/{home,config,cache}
```

Existing regression pattern to follow:

```python
result = subprocess.run(cmd, cwd=repo_root, capture_output=True, text=True)
match = re.search(r"Proof run root: (.+)", output)
run_root = Path(match.group(1).strip())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add regression coverage for isolated-state and incomplete-artifact preservation</name>
  <files>scripts/test_gdscript_proof_manifest_contract.py, scripts/test_gdscript_proof_incomplete_artifacts.py</files>
  <read_first>
    - scripts/test_gdscript_proof_manifest_contract.py
    - scripts/test_gdscript_proof_same_script_calls.py
    - scripts/gdscript-proof.sh
  </read_first>
  <behavior>
    - Test 1: a successful manifest run exposes the additive run index and the index lists wrapper JSON paths for the fixed query suite.
    - Test 2: an intentionally broken query or capture path yields a non-zero/incomplete run but still leaves `repo-meta.json`, any already-written wrappers, and the run index on disk with explicit missing/failed query metadata.
  </behavior>
  <action>First extend `scripts/test_gdscript_proof_manifest_contract.py` so it asserts the new run-level index exists and points to `repo-meta.json`, `summary.md`, and wrapper files without replacing them. Then create `scripts/test_gdscript_proof_incomplete_artifacts.py` following the existing temp-repo regression pattern: build a temporary fixture repo, run `scripts/gdscript-proof.sh` in manifest mode, intentionally trigger a query failure using the harness's existing invalid-query injection seam, assert the run exits incomplete, and verify that the run root still contains the additive run index, `repo-meta.json`, partial wrapper evidence if produced, and machine-readable failure/missing-query metadata. Clean up generated run roots at the end of each test.</action>
  <acceptance_criteria>
    - `scripts/test_gdscript_proof_manifest_contract.py` asserts a run index file exists and references wrapper JSON files
    - `scripts/test_gdscript_proof_incomplete_artifacts.py` exists
    - `scripts/test_gdscript_proof_incomplete_artifacts.py` asserts incomplete runs preserve `repo-meta.json` and the run index
    - `python3 scripts/test_gdscript_proof_manifest_contract.py && python3 scripts/test_gdscript_proof_incomplete_artifacts.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/test_gdscript_proof_manifest_contract.py && python3 scripts/test_gdscript_proof_incomplete_artifacts.py</automated>
  </verify>
  <done>Regression coverage fails if the harness stops writing the run index, stops exposing wrapper locations, or drops incomplete-run artifacts/failure metadata.</done>
</task>

<task type="auto">
  <name>task 2: update the proof runbook for the additive evidence contract</name>
  <files>docs/superpowers/proofs/gdscript-real-project-validation.md</files>
  <read_first>
    - docs/superpowers/proofs/gdscript-real-project-validation.md
    - .planning/phases/02-isolated-proof-harness/02-CONTEXT.md
    - scripts/gdscript-proof.sh
  </read_first>
  <action>Update `docs/superpowers/proofs/gdscript-real-project-validation.md` to document the exact Phase 2 contract per D-01 through D-12: `scripts/gdscript-proof.sh` remains the canonical proof entrypoint, runtime state stays under repo-owned `.artifacts/gdscript-proof/<run>/state/<slug>/...`, `queries/*.json` remains the canonical raw MCP evidence set, the new additive run-level index lives beside `aggregate-summary.md`, and incomplete repos/runs preserve partial artifacts plus machine-readable failure context instead of requiring manual database inspection. Add the run index to the artifact tree example and operator checklist using the concrete filename chosen in Plan 02-01.</action>
  <acceptance_criteria>
    - `docs/superpowers/proofs/gdscript-real-project-validation.md` contains the chosen run index filename
    - the runbook still describes `queries/*.json` as the canonical raw evidence set
    - the artifact tree documents isolated `state/<slug>/home`, `config`, and `cache` directories
    - the operator checklist mentions incomplete-run artifact inspection without manual database queries
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/test_gdscript_proof_manifest_contract.py && python3 scripts/test_gdscript_proof_incomplete_artifacts.py</automated>
  </verify>
  <done>The runbook and regression suite both reflect the same additive evidence contract, including successful and incomplete proof runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness output → documentation/tests | Docs and tests must reflect the real artifact contract, not a stale approximation |
| failing query path → artifact consumers | Incomplete evidence must be explicit so later phases do not treat partial runs as verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | R | runbook/operator flow | mitigate | Update the runbook with the exact run index filename, artifact tree, and incomplete-path behavior so operators can audit evidence consistently |
| T-02-06 | T | regression coverage | mitigate | Add automated tests that fail when wrapper locations, run index paths, or incomplete metadata disappear |
| T-02-07 | I | partial-run interpretation | mitigate | Assert incomplete runs still emit machine-readable failure/missing-query metadata so later consumers cannot mistake them for clean passes |
| T-02-08 | D | proof rerun diagnostics | accept | A failed proof run still exits non-zero, but preserving artifacts is sufficient for local diagnosis within Phase 2 scope |
</threat_model>

<verification>
- `python3 scripts/test_gdscript_proof_manifest_contract.py && python3 scripts/test_gdscript_proof_incomplete_artifacts.py`
- Read the updated runbook sections for artifact layout and confirm the documented index filename matches the harness implementation from Plan 02-01.
</verification>

<success_criteria>
- The runbook documents the additive run index, wrapper-first evidence model, and incomplete-run inspection path.
- Regression tests cover both successful manifest runs and intentionally incomplete runs.
- Maintainers can discover isolated runtime state and raw query artifacts from docs and machine-readable outputs alone.
</success_criteria>

<output>
After completion, create `.planning/phases/02-isolated-proof-harness/02-isolated-proof-harness-02-SUMMARY.md`
</output>
