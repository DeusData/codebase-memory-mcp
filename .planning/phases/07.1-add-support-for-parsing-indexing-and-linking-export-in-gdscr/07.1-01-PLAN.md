---
phase: 07.1-add-support-for-parsing-indexing-and-linking-export-in-gdscr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cbm/extract_defs.c
  - internal/cbm/helpers.h
  - internal/cbm/helpers.c
  - tests/test_extraction.c
  - tests/test_pipeline.c
autonomous: true
requirements: []
must_haves:
  truths:
    - "GDScript `@export` variables are indexed with `is_exported=true`, while non-export GDScript variables are not misclassified as exported."
    - "Exported GDScript variables are linked under the same script/class anchor naming model used by existing GDScript definitions."
    - "Native extraction and pipeline regressions prove `@export` parsing/indexing/linking behavior remains stable."
  artifacts:
    - path: "internal/cbm/extract_defs.c"
      provides: "GDScript-specific variable export detection + anchor-linked variable QN behavior"
      contains: "CBM_LANG_GDSCRIPT"
    - path: "internal/cbm/helpers.h"
      provides: "Public helper declarations for GDScript export annotation detection"
      contains: "gdscript"
    - path: "internal/cbm/helpers.c"
      provides: "AST helper logic that classifies `@export` on GDScript variable nodes"
      contains: "annotation"
    - path: "tests/test_extraction.c"
      provides: "Extraction-level regressions for `@export` and non-export variable semantics"
      contains: "@export"
    - path: "tests/test_pipeline.c"
      provides: "End-to-end indexing assertions for exported-variable flags and linkage"
      contains: "gdscript"
  key_links:
    - from: "internal/cbm/extract_defs.c"
      to: "internal/cbm/helpers.c"
      via: "GDScript variable extraction path invokes export-annotation helper"
      pattern: "export"
    - from: "internal/cbm/extract_defs.c"
      to: "tests/test_extraction.c"
      via: "Unit extraction assertions cover annotation-derived export state"
      pattern: "is_exported"
    - from: "internal/cbm/extract_defs.c"
      to: "tests/test_pipeline.c"
      via: "Pipeline persistence checks confirm graph-visible exported-variable semantics"
      pattern: "Variable"
---

<objective>
Add production-grade support for parsing, indexing, and linking GDScript `@export` variables so exported-variable semantics are accurate and queryable.

Purpose: close the new inserted phase scope without changing unrelated GDScript semantics.
Output: extractor + helper updates with regression-backed proof in native extraction and pipeline tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-add-support-for-parsing-indexing-and-linking-export-in-gdscr/07.1-RESEARCH.md
@.planning/phases/07.1-add-support-for-parsing-indexing-and-linking-export-in-gdscr/07.1-VALIDATION.md
@internal/cbm/lang_specs.c
@internal/cbm/extract_defs.c
@internal/cbm/helpers.h
@internal/cbm/helpers.c
@tests/test_extraction.c
@tests/test_pipeline.c

<interfaces>
From `internal/cbm/lang_specs.c`:
```c
static const char *gdscript_var_types[] = {"variable_statement", "export_variable_statement",
                                           "onready_variable_statement", "const_statement", NULL};
```

From `internal/cbm/extract_defs.c`:
```c
static void push_var_def(CBMExtractCtx *ctx, const char *name, TSNode node);
static void extract_variables(CBMExtractCtx *ctx, TSNode root, const CBMLangSpec *spec);
```

From `internal/cbm/helpers.h`:
```c
const char *cbm_gdscript_anchor_qn(CBMArena *a, TSNode root, const char *source,
                                   const char *project, const char *rel_path);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add failing regressions for GDScript `@export` semantics and variable linkage</name>
  <files>tests/test_extraction.c, tests/test_pipeline.c</files>
  <read_first>
    - codemap.md
    - tests/test_extraction.c
    - tests/test_pipeline.c
    - internal/cbm/extract_defs.c
    - internal/cbm/lang_specs.c
  </read_first>
  <behavior>
    - Test 1: `@export var speed = 10` is indexed as a `Variable` with exported metadata set true.
    - Test 2: plain `var hp = 100` in the same fixture is indexed as a `Variable` with exported metadata set false.
    - Test 3: exported-variable qualified naming/linkage remains under the script/class anchor model instead of detached module-level naming.
  </behavior>
  <action>Create or extend GDScript-focused regressions in `tests/test_extraction.c` and `tests/test_pipeline.c` so they assert both positive and negative export classification for variables (`@export` vs plain `var`) and assert anchor-linked variable naming/linkage for GDScript definitions. Keep fixture snippets minimal and colocated with existing GDScript test blocks. Name tests with explicit `gdscript_export` prefixes for easy grep and future triage.</action>
  <acceptance_criteria>
    - `tests/test_extraction.c` contains at least one new test case with literal `@export var` and explicit assertions that differentiate exported vs non-exported variable behavior.
    - `tests/test_pipeline.c` contains at least one new GDScript pipeline regression that validates exported-variable graph-visible semantics.
    - New tests are deterministic and do not depend on network, wall-clock timing, or external repos.
  </acceptance_criteria>
  <verify>
    <automated>python3 - &lt;&lt;'PY'
from pathlib import Path
tx = Path('tests/test_extraction.c').read_text()
tp = Path('tests/test_pipeline.c').read_text()
assert '@export var' in tx
assert 'gdscript_export' in tx
assert 'gdscript_export' in tp
print('PASS')
PY</automated>
  </verify>
  <done>Regression tests exist and codify the expected `@export` parsing/indexing/linking behavior before production code changes are finalized.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement GDScript export-aware variable indexing and anchor linkage</name>
  <files>internal/cbm/extract_defs.c, internal/cbm/helpers.h, internal/cbm/helpers.c</files>
  <read_first>
    - codemap.md
    - internal/cbm/extract_defs.c
    - internal/cbm/helpers.h
    - internal/cbm/helpers.c
    - internal/cbm/lang_specs.c
    - tests/test_extraction.c
    - tests/test_pipeline.c
  </read_first>
  <behavior>
    - Test 1: GDScript variable export state is derived from `@export`/`export_variable_statement` semantics, not generic name heuristics.
    - Test 2: GDScript variable QNs/linkage align with existing anchor-based GDScript definition wiring.
    - Test 3: Existing non-GDScript export behavior remains unchanged.
  </behavior>
  <action>Implement a GDScript-specific export detector in helpers (e.g., node-kind + annotation ancestry check for `annotation`/`annotations` containing `export`) and invoke it from GDScript variable extraction in `push_var_def` / related call sites. For `CBM_LANG_GDSCRIPT`, set `def.is_exported` from this detector and compute variable qualified names/linkage using the current script/class anchor conventions (`cbm_gdscript_anchor_qn` and existing enclosing class context). Keep behavior for all other languages on the current `cbm_is_exported(...)` path unchanged.</action>
  <acceptance_criteria>
    - `internal/cbm/helpers.h` declares a dedicated helper for GDScript variable export detection.
    - `internal/cbm/helpers.c` implements annotation/node-based `@export` detection used by extraction.
    - `internal/cbm/extract_defs.c` uses the new helper in the GDScript variable path and preserves legacy behavior for non-GDScript languages.
    - `rtk make -f Makefile.cbm test` exits 0 with the new regression cases passing.
  </acceptance_criteria>
  <verify>
    <automated>rtk make -f Makefile.cbm test</automated>
  </verify>
  <done>GDScript `@export` variables are parsed, indexed, and anchor-linked correctly, with full-suite regression coverage passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted `.gd` source -> extractor AST walk | Arbitrary repository code is parsed and converted into indexed definitions/properties |
| extraction output -> persisted graph properties | Incorrect export classification can produce misleading graph metadata and downstream query answers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Tampering | `internal/cbm/extract_defs.c` GDScript variable path | mitigate | Use explicit AST-based `@export` detection instead of permissive fallback logic; cover with positive + negative regressions. |
| T-07.1-02 | Integrity | graph persisted `is_exported` variable metadata | mitigate | Add pipeline-level assertions that exported/non-exported variables persist correct metadata and linkage in indexed graph output. |
| T-07.1-03 | Denial of Service | recursive AST checks in helper logic | accept | Scope helper to bounded parent/child scans used in existing extraction patterns; no external I/O or unbounded allocations introduced. |
</threat_model>

<verification>
- `rtk make -f Makefile.cbm test` passes.
- Extraction tests explicitly cover `@export` and non-export variable behavior.
- Pipeline tests confirm persisted exported-variable behavior and linkage remains queryable.
</verification>

<success_criteria>
- `@export` variables are no longer treated the same as plain variables for export status.
- GDScript variable definitions remain linked under the anchor/class naming model.
- Native and script verification remain green after the change.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-add-support-for-parsing-indexing-and-linking-export-in-gdscr/07.1-add-support-for-parsing-indexing-and-linking-export-in-gdscr-01-SUMMARY.md`
</output>
