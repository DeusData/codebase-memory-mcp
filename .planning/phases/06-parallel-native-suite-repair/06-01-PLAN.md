---
phase: 06-parallel-native-suite-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/pipeline.c
  - src/pipeline/pipeline_incremental.c
  - src/pipeline/pass_calls.c
  - tests/test_pipeline.c
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "`tests/test_pipeline.c:4361` `pipeline_fastapi_depends_edges` passes when the full suite is run with `CBM_FORCE_PIPELINE_MODE=parallel`."
    - "FastAPI `Depends(func)` CALLS edges are emitted on the full parallel pipeline path instead of only on the sequential path."
    - "The repair preserves existing sequential behavior and the already-verified forced-parallel incremental path."
  artifacts:
    - path: "src/pipeline/pipeline.c"
      provides: "Full parallel pipeline orchestration keeps cache-backed FastAPI edge extraction alive before cache teardown"
      contains: "cbm_pipeline_pass_fastapi_depends"
    - path: "src/pipeline/pipeline_incremental.c"
      provides: "Incremental parallel orchestration matches the full-path FastAPI edge contract"
      contains: "cbm_pipeline_pass_fastapi_depends"
    - path: "tests/test_pipeline.c"
      provides: "Regression coverage for FastAPI Depends edges under forced parallel"
      contains: "pipeline_fastapi_depends_edges"
  key_links:
    - from: "src/pipeline/pipeline.c"
      to: "src/pipeline/pass_calls.c"
      via: "cache-backed FastAPI Depends post-pass on the full parallel path"
      pattern: "cbm_pipeline_pass_fastapi_depends"
    - from: "src/pipeline/pipeline_incremental.c"
      to: "src/pipeline/pass_calls.c"
      via: "cache-backed FastAPI Depends post-pass on the incremental parallel path"
      pattern: "cbm_pipeline_pass_fastapi_depends"
    - from: "tests/test_pipeline.c"
      to: "src/pipeline/pipeline.c"
      via: "forced parallel full-suite regression for FastAPI Depends edges"
      pattern: "CBM_FORCE_PIPELINE_MODE"
---

<objective>
Close the deferred Phase 03 native-suite gap by restoring FastAPI `Depends()` CALLS edge generation on the parallel pipeline path.

Purpose: Make the full forced-parallel native suite green again without changing the supported FastAPI edge contract or reopening the Phase 03 worker-lifetime fix.
Output: One focused pipeline repair plus direct regression coverage for forced-parallel FastAPI dependency edges.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-repo-semantic-verification/deferred-items.md
@.planning/phases/03-real-repo-semantic-verification/03-04-PLAN.md
@.planning/phases/03-real-repo-semantic-verification/03-VERIFICATION.md
@.planning/phases/06-parallel-native-suite-repair/06-RESEARCH.md
@.planning/phases/06-parallel-native-suite-repair/06-VALIDATION.md
@src/pipeline/pipeline.c
@src/pipeline/pipeline_incremental.c
@src/pipeline/pass_calls.c
@tests/test_pipeline.c

<interfaces>
From `src/pipeline/pass_calls.c`:
```c
void cbm_pipeline_pass_fastapi_depends(cbm_pipeline_ctx_t *ctx, const cbm_file_info_t *files,
                                       int file_count);
```

From `src/pipeline/pipeline.c`:
```c
rc = cbm_parallel_extract(ctx, files, file_count, cache, &shared_ids, worker_count);
rc = cbm_build_registry_from_cache(ctx, files, file_count, cache);
rc = cbm_parallel_resolve(ctx, files, file_count, cache, &shared_ids, worker_count);
```

From `src/pipeline/pipeline_incremental.c`:
```c
cbm_parallel_extract(ctx, changed_files, ci, cache, &shared_ids, worker_count);
cbm_build_registry_from_cache(ctx, changed_files, ci, cache);
cbm_parallel_resolve(ctx, changed_files, ci, cache, &shared_ids, worker_count);
```

From `tests/test_pipeline.c`:
```c
cbm_store_find_edges_by_type(s, proj, "CALLS", &edges, &edge_count);
if (edges[i].properties_json && strstr(edges[i].properties_json, "fastapi_depends")) {
    found_depends_edge = true;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: make the forced-parallel FastAPI Depends gap directly reproducible in native tests</name>
  <files>tests/test_pipeline.c</files>
  <read_first>
    - codemap.md
    - src/codemap.md
    - .planning/phases/03-real-repo-semantic-verification/deferred-items.md
    - .planning/phases/06-parallel-native-suite-repair/06-RESEARCH.md
    - tests/test_pipeline.c
    - src/pipeline/pipeline.c
  </read_first>
  <behavior>
    - Keep the existing `pipeline_fastapi_depends_edges` coverage for the FastAPI `Depends(get_current_user)` edge contract.
    - Add explicit forced-parallel coverage that sets `CBM_FORCE_PIPELINE_MODE=parallel`, runs the same fixture through `cbm_pipeline_run()`, restores the environment afterward, and proves a `CALLS` edge with `"strategy":"fastapi_depends"` still exists.
    - The regression must fail again if the parallel path skips FastAPI Depends edge creation, not just if the sequential path breaks.
  </behavior>
  <action>Extend `tests/test_pipeline.c` around the existing FastAPI Depends regression so the deferred gap is exercised intentionally under forced parallel. Reuse the current `auth.py` / `routes.py` fixture shape and the same final store query for `CALLS` edges with `fastapi_depends` properties. Add a dedicated forced-parallel test rather than weakening the current assertion. The new test must save the prior `CBM_FORCE_PIPELINE_MODE` value, set it to the exact string `parallel`, run `cbm_pipeline_run(p)`, restore the prior environment state on every exit path, and assert that at least one returned CALLS edge contains `"strategy":"fastapi_depends"`. Register the new regression in the suite near the existing FastAPI pipeline tests.</action>
  <acceptance_criteria>
    - `tests/test_pipeline.c` contains a new forced-parallel FastAPI regression that references `CBM_FORCE_PIPELINE_MODE` and `fastapi_depends`.
    - The test restores the environment after execution instead of leaking `CBM_FORCE_PIPELINE_MODE` into later tests.
    - `tests/test_pipeline.c` suite registration includes the new forced-parallel FastAPI regression.
  </acceptance_criteria>
  <verify>
    <automated>CBM_FORCE_PIPELINE_MODE=parallel rtk make -f Makefile.cbm test</automated>
  </verify>
  <done>The native suite contains direct regression coverage for the exact forced-parallel FastAPI edge gap instead of depending on an incidental failure in the existing test mix.</done>
</task>

<task type="auto">
  <name>task 2: run the shared FastAPI Depends pass on both parallel pipeline branches before cache teardown</name>
  <files>src/pipeline/pipeline.c, src/pipeline/pipeline_incremental.c, src/pipeline/pass_calls.c</files>
  <read_first>
    - codemap.md
    - src/codemap.md
    - .planning/phases/06-parallel-native-suite-repair/06-RESEARCH.md
    - src/pipeline/pipeline.c
    - src/pipeline/pipeline_incremental.c
    - src/pipeline/pass_calls.c
    - tests/test_pipeline.c
  </read_first>
  <action>Repair the parallel orchestration so it reuses the existing `cbm_pipeline_pass_fastapi_depends()` implementation while the extracted `CBMFileResult **cache` is still alive. In `src/pipeline/pipeline.c`, after `cbm_parallel_resolve()` succeeds and before the `cache[i]` free loop, temporarily make the cache visible through `ctx->result_cache`, invoke `cbm_pipeline_pass_fastapi_depends(ctx, files, file_count)`, then continue with infra extraction and cleanup without leaving stale cache pointers behind. Apply the same contract in `src/pipeline/pipeline_incremental.c` for the `changed_files` parallel branch before the cache free loop there as well. Keep the FastAPI Depends scanning logic in `src/pipeline/pass_calls.c` as the single source of truth; do not duplicate the regex scan or edge insertion logic inside `pass_parallel.c`. Adjust `src/pipeline/pass_calls.c` only if a tiny helper or null-safe cache bookkeeping is needed to support both call sites.</action>
  <acceptance_criteria>
    - `src/pipeline/pipeline.c` contains `cbm_pipeline_pass_fastapi_depends` in the full parallel flow before cache teardown.
    - `src/pipeline/pipeline_incremental.c` contains `cbm_pipeline_pass_fastapi_depends` in the incremental parallel flow before cache teardown.
    - `src/pipeline/pass_calls.c` remains the only file that contains the `"strategy":"fastapi_depends"` edge insertion logic.
    - `CBM_FORCE_PIPELINE_MODE=parallel rtk make -f Makefile.cbm test` exits successfully.
    - `rtk make -f Makefile.cbm test` exits successfully after the repair.
  </acceptance_criteria>
  <verify>
    <automated>rtk make -f Makefile.cbm test && CBM_FORCE_PIPELINE_MODE=parallel rtk make -f Makefile.cbm test</automated>
  </verify>
  <done>The full and incremental parallel paths produce the same FastAPI Depends edge behavior as the sequential path, and the deferred native-suite failure is closed without reopening prior Phase 03 work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env-driven mode forcing -> pipeline orchestration | Local env input can force the pipeline into the parallel branch that must preserve supported semantic passes |
| parallel result cache -> FastAPI edge emission | Cache-backed semantic work must finish before cache teardown or edges can disappear silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | forced-parallel native verification path | mitigate | Add and keep a dedicated forced-parallel FastAPI regression so suite-level debt cannot hide behind sequential-only coverage. |
| T-06-02 | Tampering | cache-backed FastAPI dependency edge generation | mitigate | Invoke `cbm_pipeline_pass_fastapi_depends()` while cache entries are still alive on both parallel branches and keep one semantic implementation. |

</threat_model>

<verification>
- `CBM_FORCE_PIPELINE_MODE=parallel rtk make -f Makefile.cbm test` passes and no longer fails at `pipeline_fastapi_depends_edges`.
- `rtk make -f Makefile.cbm test` still passes, proving the repair did not regress the normal suite path.
</verification>

<success_criteria>
- The deferred forced-parallel full-suite failure is gone.
- FastAPI `Depends()` CALLS edges are preserved on the full parallel path.
- Incremental parallel orchestration matches the repaired full-path contract.
- Regression coverage makes the gap directly reproducible if it returns.
</success_criteria>

<output>
After completion, create `.planning/phases/06-parallel-native-suite-repair/06-parallel-native-suite-repair-01-SUMMARY.md`
</output>
