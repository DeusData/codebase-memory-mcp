---
phase: 03-real-repo-semantic-verification
plan: 04
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/pipeline/pass_parallel.c
  - tests/test_pipeline.c
autonomous: true
requirements:
  - SEM-06
gap_closure: true
must_haves:
  truths:
    - "Forcing `CBM_FORCE_PIPELINE_MODE=parallel` through the incremental new-file path completes without an AddressSanitizer heap-use-after-free."
    - "Forced parallel runs keep requested-versus-actual mode behavior deterministic instead of crashing mid-run."
    - "The native test suite catches this worker-lifetime regression before Phase 03 proof runs are used again."
  artifacts:
    - path: "src/pipeline/pass_parallel.c"
      provides: "Worker-lifetime parser/slab cleanup contract"
      contains: "cbm_reset_thread_parser"
    - path: "tests/test_pipeline.c"
      provides: "Forced-parallel incremental regression"
      contains: "CBM_FORCE_PIPELINE_MODE"
  key_links:
    - from: "tests/test_pipeline.c"
      to: "src/pipeline/pipeline.c"
      via: "forced-parallel incremental pipeline run"
      pattern: "CBM_FORCE_PIPELINE_MODE"
    - from: "src/pipeline/pass_parallel.c"
      to: "internal/cbm/cbm.h"
      via: "parser reset/destroy ordering"
      pattern: "cbm_reset_thread_parser|cbm_destroy_thread_parser"
---

<objective>
Close the Phase 03 UAT blocker caused by forced-parallel Tree-sitter lifetime handling.

Purpose: Restore deterministic forced-parallel behavior by fixing the worker cleanup order that frees parser-owned memory too early.
Output: One native lifetime fix plus one regression covering the incremental new-file-added forced-parallel path.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-repo-semantic-verification/03-UAT.md
@.planning/debug/phase-03-uat-forced-mode-asan.md
@src/pipeline/pass_parallel.c
@src/pipeline/pipeline.c
@internal/cbm/cbm.h
@internal/cbm/cbm.c
@src/foundation/slab_alloc.h
@tests/test_pipeline.c

<interfaces>
From `internal/cbm/cbm.h`:
```c
void cbm_reset_thread_parser(void);
void cbm_destroy_thread_parser(void);
```

From `src/foundation/slab_alloc.h`:
```c
void cbm_slab_reset_thread(void);
void cbm_slab_destroy_thread(void);
void cbm_slab_reclaim(void);
```

From `src/pipeline/pipeline.c`:
```c
const char *requested_mode =
    cbm_safe_getenv("CBM_FORCE_PIPELINE_MODE", requested_mode_buf, sizeof(requested_mode_buf), NULL);
rc = cbm_pipeline_select_mode(requested_mode, worker_count, file_count, &mode_selection);
```

From `tests/test_pipeline.c`:
```c
TEST(incremental_new_file_added) {
    p = cbm_pipeline_new(g_incr_tmpdir, g_incr_dbpath, CBM_MODE_FULL);
    ASSERT_EQ(cbm_pipeline_run(p), 0);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add a forced-parallel incremental regression for the ASan crash path</name>
  <read_first>
    - codemap.md
    - src/codemap.md
    - internal/codemap.md
    - .planning/phases/03-real-repo-semantic-verification/03-UAT.md
    - .planning/debug/phase-03-uat-forced-mode-asan.md
    - tests/test_pipeline.c
    - src/pipeline/pipeline.c
  </read_first>
  <files>tests/test_pipeline.c</files>
  <behavior>
    - Test 1: a worker-capable machine can run the incremental new-file-added path with `CBM_FORCE_PIPELINE_MODE=parallel` and `cbm_pipeline_run()` returns `0` for both the initial and follow-up run.
    - Test 2: the regression restores the environment after the test and skips only when `cbm_default_worker_count(true) <= 1`, never by silently falling back to sequential.
  </behavior>
  <action>Extend `tests/test_pipeline.c` with a dedicated regression for the exact UAT path, using the existing `setup_incremental_repo()` fixture and the `incremental_new_file_added` flow as the base. Set `CBM_FORCE_PIPELINE_MODE=parallel` for the scope of the test, run the initial full index, add the new file, rerun the pipeline, and assert the run completes successfully with indexed nodes still queryable afterward. Keep this focused on the incremental/new-file-added reproduction path named in the UAT blocker, and preserve deterministic forced-mode semantics from the existing Phase 03 mode-selection contract instead of allowing fallback. This task implements the gap's required regression and preserves D-09 through D-11 by proving the parallel path itself works.</action>
  <verify>
    <automated>CBM_FORCE_PIPELINE_MODE=parallel make -f Makefile.cbm test</automated>
  </verify>
  <done>The native test suite contains a focused forced-parallel incremental regression that would fail again if the worker-lifetime bug reappears.</done>
</task>

<task type="auto">
  <name>task 2: keep Tree-sitter parser and slab state alive for the full worker lifetime</name>
  <read_first>
    - codemap.md
    - src/codemap.md
    - internal/codemap.md
    - .planning/phases/03-real-repo-semantic-verification/03-UAT.md
    - .planning/debug/phase-03-uat-forced-mode-asan.md
    - src/pipeline/pass_parallel.c
    - internal/cbm/cbm.h
    - internal/cbm/cbm.c
    - src/foundation/slab_alloc.h
  </read_first>
  <files>src/pipeline/pass_parallel.c</files>
  <action>Update `extract_worker()` so parser/slab teardown matches the documented worker-lifetime contract from the diagnosis. After `cbm_free_tree(result)` and result-cache storage, do not bulk free thread-local Tree-sitter allocator pages inside the per-file loop; remove the per-file `cbm_destroy_thread_parser()` + `cbm_slab_reclaim()` cleanup that created the heap-use-after-free. Keep parser/slab state alive across files for the worker lifetime, then perform final thread cleanup at worker exit by destroying the thread-local parser before destroying the thread slab state. Do not widen scope into proof-harness changes, byte-for-byte parity checks, or unrelated memory optimizations; this gap closes only the forced-parallel lifetime bug described in 03-UAT.md.</action>
  <verify>
    <automated>CBM_FORCE_PIPELINE_MODE=parallel make -f Makefile.cbm test</automated>
  </verify>
  <done>Forced parallel incremental runs no longer crash under AddressSanitizer because parser-owned allocations are not invalidated mid-worker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test environment → pipeline mode selection | Untrusted env input forces the pipeline into the parallel path that must remain deterministic |
| parallel worker → Tree-sitter allocator state | Worker cleanup crosses into parser-owned memory that must not be freed while still reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Denial of Service | `src/pipeline/pass_parallel.c` | mitigate | Remove per-file parser/slab destruction so forced parallel cannot crash the process mid-index from a use-after-free. |
| T-03-12 | Tampering | thread-local parser/slab lifetime | mitigate | Keep parser-owned allocations valid for the full worker lifetime and destroy them only in worker-exit cleanup order. |
| T-03-13 | Repudiation | `tests/test_pipeline.c` | mitigate | Add a dedicated forced-parallel incremental regression so this UAT blocker is reproducible and automatically caught in the default native test path. |

</threat_model>

<verification>
- `CBM_FORCE_PIPELINE_MODE=parallel make -f Makefile.cbm test` passes without an AddressSanitizer heap-use-after-free.
- The new incremental forced-parallel regression remains in `tests/test_pipeline.c` and exercises the new-file-added path specifically named in the UAT gap.
</verification>

<success_criteria>
- Forced parallel no longer aborts Phase 03 verification with a Tree-sitter heap-use-after-free.
- Requested `parallel` mode reaches the real parallel path deterministically on worker-capable machines.
- The exact UAT crash path is covered by an automated regression.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-repo-semantic-verification/03-real-repo-semantic-verification-04-SUMMARY.md`
</output>
