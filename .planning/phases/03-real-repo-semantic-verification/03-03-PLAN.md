---
phase: 03-real-repo-semantic-verification
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - scripts/test_gdscript_proof_manifest_contract.py
  - scripts/test_gdscript_proof_semantic_parity.py
  - Makefile.cbm
  - docs/superpowers/proofs/gdscript-real-project-validation.md
  - docs/superpowers/proofs/gdscript-good-tier-checklist.md
autonomous: true
requirements:
  - SEM-01
  - SEM-02
  - SEM-03
  - SEM-04
  - SEM-05
  - SEM-06
must_haves:
  truths:
    - "Maintainer can rerun automated proof regressions and catch broken semantic-parity artifacts before using them for approval."
    - "The standard repo test path exercises the new proof-layer regressions."
    - "The operator docs and checklist say exactly how to review counts, samples, and sequential-versus-parallel parity."
  artifacts:
    - path: "scripts/test_gdscript_proof_semantic_parity.py"
      provides: "Proof-harness parity regression"
      contains: "semantic-parity.json"
    - path: "Makefile.cbm"
      provides: "Standard test-path wiring"
      contains: "test_gdscript_proof_semantic_parity.py"
    - path: "docs/superpowers/proofs/gdscript-good-tier-checklist.md"
      provides: "Phase 03 review checklist"
      contains: "sequential"
  key_links:
    - from: "Makefile.cbm"
      to: "scripts/test_gdscript_proof_semantic_parity.py"
      via: "test target"
      pattern: "test_gdscript_proof_semantic_parity.py"
    - from: "docs/superpowers/proofs/gdscript-good-tier-checklist.md"
      to: "docs/superpowers/proofs/gdscript-real-project-validation.md"
      via: "operator review instructions"
      pattern: "semantic parity"
---

<objective>
Lock Phase 03 in with regression-backed proof checks and operator documentation.

Purpose: Make the new semantic evidence durable, repeatable, and easy to review through the normal test and proof workflow.
Output: Proof regressions wired into `make test` plus updated runbook/checklist guidance.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-repo-semantic-verification/03-CONTEXT.md
@.planning/phases/03-real-repo-semantic-verification/03-RESEARCH.md
@scripts/test_gdscript_proof_manifest_contract.py
@scripts/test_gdscript_proof_same_script_calls.py
@Makefile.cbm
@docs/superpowers/proofs/gdscript-real-project-validation.md
@docs/superpowers/proofs/gdscript-good-tier-checklist.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add proof-harness regressions for dual-mode semantic evidence and wire them into the default test path</name>
  <read_first>
    - codemap.md
    - scripts/codemap.md
    - scripts/test_gdscript_proof_manifest_contract.py
    - scripts/test_gdscript_proof_same_script_calls.py
    - scripts/gdscript-proof.sh
    - Makefile.cbm
    - .planning/phases/03-real-repo-semantic-verification/03-CONTEXT.md
  </read_first>
  <files>scripts/test_gdscript_proof_manifest_contract.py, scripts/test_gdscript_proof_semantic_parity.py, Makefile.cbm</files>
  <behavior>
    - Test 1: the manifest-contract regression can discover mode-aware run-index metadata and additive parity artifacts from disk.
    - Test 2: a dedicated semantic-parity regression proves the harness writes `semantic-parity.json` with SEM-01 samples plus SEM-02..SEM-05 behavior entries.
    - Test 3: `make -f Makefile.cbm test` runs the new proof regressions automatically.
  </behavior>
  <action>Create `scripts/test_gdscript_proof_semantic_parity.py` using the existing subprocess-and-temp-repo proof-test pattern. The regression must run `scripts/gdscript-proof.sh`, parse `Proof run root: ...`, and assert that `semantic-parity.json` plus `semantic-parity.md` exist, include entries for `SEM-01` through `SEM-06`, and expose class/method counts plus representative samples. Extend `scripts/test_gdscript_proof_manifest_contract.py` to assert the new `requested_mode`, `actual_mode`, and `semantic_pairs` metadata. Update `Makefile.cbm` so the default `test` target runs `scripts/test_gdscript_proof_manifest_contract.py`, `scripts/test_gdscript_proof_same_script_calls.py`, and `scripts/test_gdscript_proof_semantic_parity.py` in that order. Do not create a second test command; keep the standard path additive and repo-owned.</action>
  <acceptance_criteria>
    - `scripts/test_gdscript_proof_semantic_parity.py` exists.
    - `scripts/test_gdscript_proof_semantic_parity.py` contains the literal string `semantic-parity.json`.
    - `scripts/test_gdscript_proof_manifest_contract.py` contains the literal strings `requested_mode`, `actual_mode`, and `semantic_pairs`.
    - `Makefile.cbm` contains lines invoking `python3 -B scripts/test_gdscript_proof_manifest_contract.py` and `python3 -B scripts/test_gdscript_proof_semantic_parity.py` under the `test:` target.
    - The standard `make -f Makefile.cbm test` path exercises all proof regressions without requiring a new target name.
  </acceptance_criteria>
  <verify>
    <automated>make -f Makefile.cbm test</automated>
  </verify>
  <done>Proof regressions cover the new dual-mode semantic evidence and run automatically in the normal repository test path.</done>
</task>

<task type="auto">
  <name>task 2: document the exact Phase 03 review bar in the runbook and checklist</name>
  <read_first>
    - codemap.md
    - scripts/codemap.md
    - docs/superpowers/proofs/gdscript-real-project-validation.md
    - docs/superpowers/proofs/gdscript-good-tier-checklist.md
    - docs/superpowers/proofs/gdscript-good-tier-manifest.json
    - .planning/phases/03-real-repo-semantic-verification/03-CONTEXT.md
  </read_first>
  <files>docs/superpowers/proofs/gdscript-real-project-validation.md, docs/superpowers/proofs/gdscript-good-tier-checklist.md</files>
  <action>Update the proof runbook and checklist to match the implemented Phase 03 approval bar. Document that `scripts/gdscript-proof.sh` remains the only approval-bearing workflow (D-01, D-02, D-03), that approval coverage must include all four pinned manifest repos in both sequential and parallel mode (D-04, D-05), that SEM-01 review requires non-zero class and method counts plus representative class/method samples (D-06, D-07), and that SEM-06 is judged by semantic comparison rather than byte-for-byte artifact equality or aggregate pass/fail only (D-09, D-10, D-11). Add explicit checklist bullets for `semantic-parity.json`, `semantic-parity.md`, and the requirement to inspect wrapper JSON when parity results are `incomplete`.</action>
  <acceptance_criteria>
    - `docs/superpowers/proofs/gdscript-real-project-validation.md` states that `scripts/gdscript-proof.sh` remains the approval-bearing workflow.
    - `docs/superpowers/proofs/gdscript-real-project-validation.md` contains the filenames `semantic-parity.json` and `semantic-parity.md`.
    - `docs/superpowers/proofs/gdscript-good-tier-checklist.md` mentions all four manifest repos and both `sequential` and `parallel` modes.
    - `docs/superpowers/proofs/gdscript-good-tier-checklist.md` explicitly requires class/method counts plus representative samples for SEM-01.
    - The docs do not introduce byte-for-byte equality as the parity bar.
  </acceptance_criteria>
  <verify>
    <automated>make -f Makefile.cbm test</automated>
    <manual>grep -n "approval-bearing workflow\|semantic-parity.json\|semantic-parity.md\|all four manifest repos\|sequential\|parallel\|representative samples" docs/superpowers/proofs/gdscript-real-project-validation.md docs/superpowers/proofs/gdscript-good-tier-checklist.md</manual>
  </verify>
  <done>Operators have precise documentation and checklist gates for Phase 03 semantic review and parity interpretation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof implementation → regression suite | Regressions must catch artifact-contract drift before human review |
| runbook/checklist → operator decisions | Docs define the approval bar maintainers will rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Repudiation | proof regressions | mitigate | Assert parity metadata and semantic summary files from disk artifacts, not just exit codes, so approval evidence stays inspectable. |
| T-03-09 | Tampering | `Makefile.cbm` test path | mitigate | Wire the new proof regressions into the default `test` target so broken parity reporting cannot bypass standard verification. |
| T-03-10 | Repudiation | runbook/checklist guidance | mitigate | Document the exact Phase 03 bar: four repos, two modes, counts plus samples, semantic parity not byte equality. |

</threat_model>

<verification>
- `make -f Makefile.cbm test` passes.
- `grep -n "semantic-parity.json\|semantic-parity.md\|sequential\|parallel" docs/superpowers/proofs/gdscript-real-project-validation.md docs/superpowers/proofs/gdscript-good-tier-checklist.md scripts/test_gdscript_proof_manifest_contract.py scripts/test_gdscript_proof_semantic_parity.py Makefile.cbm` shows the regression and documentation contract.
</verification>

<success_criteria>
- Proof-layer regressions cover the new dual-mode evidence.
- The normal repo test path runs those regressions.
- The runbook and checklist tell maintainers exactly how to review Phase 03 semantic evidence honestly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-repo-semantic-verification/03-real-repo-semantic-verification-03-SUMMARY.md`
</output>
