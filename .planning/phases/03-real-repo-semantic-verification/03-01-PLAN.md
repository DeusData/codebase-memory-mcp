---
phase: 03-real-repo-semantic-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/pipeline.c
  - src/pipeline/pipeline_internal.h
  - tests/test_parallel.c
autonomous: true
requirements:
  - SEM-06
must_haves:
  truths:
    - "The pipeline exposes a deterministic sequential/parallel override that the canonical proof workflow can consume in later plans."
    - "The pipeline records which execution mode was requested and which mode actually ran."
    - "Forced-mode behavior is covered by native regressions so harness parity work can rely on it."
  artifacts:
    - path: "src/pipeline/pipeline.c"
      provides: "Deterministic execution-mode selection"
      contains: "CBM_FORCE_PIPELINE_MODE"
    - path: "src/pipeline/pipeline_internal.h"
      provides: "Mode-selection contract shared with tests"
      contains: "cbm_pipeline_mode"
    - path: "tests/test_parallel.c"
      provides: "Regression coverage for forced sequential and forced parallel selection"
      contains: "forced_pipeline_mode"
  key_links:
    - from: "src/pipeline/pipeline.c"
      to: "tests/test_parallel.c"
      via: "shared mode-selection helper"
      pattern: "CBM_FORCE_PIPELINE_MODE"
---

<objective>
Create a deterministic sequential-versus-parallel selection contract the proof harness can drive for the same repo.

Purpose: Resolve the Phase 03 design blocker behind SEM-06 before adding proof-layer parity evidence.
Output: Native mode-override plumbing plus native regression coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-repo-semantic-verification/03-CONTEXT.md
@.planning/phases/03-real-repo-semantic-verification/03-RESEARCH.md
@src/pipeline/pipeline.c
@src/pipeline/pipeline_internal.h
@tests/test_parallel.c

<interfaces>
From `src/pipeline/pipeline.c` today:

```c
int worker_count = cbm_default_worker_count(true);
#define MIN_FILES_FOR_PARALLEL 50
rc = (worker_count > SKIP_ONE && file_count > MIN_FILES_FOR_PARALLEL)
         ? run_parallel_pipeline(p, &ctx, files, file_count, worker_count, &t)
         : run_sequential_pipeline(p, &ctx, files, file_count, &t);
```

The implementation in this plan must keep the `auto` rule above intact while adding an explicit override contract the harness can set.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add deterministic pipeline mode override selection</name>
  <read_first>
    - codemap.md
    - src/pipeline/codemap.md
    - src/pipeline/pipeline.c
    - src/pipeline/pipeline_internal.h
    - .planning/phases/03-real-repo-semantic-verification/03-CONTEXT.md
    - .planning/phases/03-real-repo-semantic-verification/03-RESEARCH.md
  </read_first>
  <files>src/pipeline/pipeline.c, src/pipeline/pipeline_internal.h</files>
  <behavior>
    - Test 1: unset or `auto` keeps the existing `worker_count &gt; 1 && file_count &gt; 50` rule.
    - Test 2: `CBM_FORCE_PIPELINE_MODE=sequential` always selects the sequential path.
    - Test 3: `CBM_FORCE_PIPELINE_MODE=parallel` bypasses only the 50-file threshold, still requires `worker_count &gt; 1`, and never silently falls back to sequential.
    - Test 4: invalid override values are reported explicitly and default to `auto` instead of changing behavior silently.
  </behavior>
  <action>Per D-09, D-10, and D-11, extract the mode-selection decision from `cbm_pipeline_run()` into a shared helper declared in `src/pipeline/pipeline_internal.h`. Read environment variable `CBM_FORCE_PIPELINE_MODE` with accepted values `auto`, `sequential`, and `parallel`. Preserve the current auto rule exactly. For `sequential`, bypass the parallel threshold and run `run_sequential_pipeline(...)`. For `parallel`, bypass the `MIN_FILES_FOR_PARALLEL` gate but still require `worker_count &gt; 1`; if that prerequisite is not met, return a deterministic non-zero error and log a machine-readable selection/failure message instead of silently switching modes. Log one explicit selection event containing the literal key `pipeline.mode_selection` and fields for requested mode, selected mode, worker count, file count, and whether the decision was forced. Keep existing pipeline behavior unchanged when the env var is absent.</action>
  <acceptance_criteria>
    - `src/pipeline/pipeline.c` contains the literal string `CBM_FORCE_PIPELINE_MODE`.
    - `src/pipeline/pipeline.c` contains the literal log key `pipeline.mode_selection`.
    - `src/pipeline/pipeline.c` still contains the exact auto-threshold constant `MIN_FILES_FOR_PARALLEL 50`.
    - `src/pipeline/pipeline_internal.h` exports a mode-selection helper or enum containing the token `cbm_pipeline_mode`.
    - No new CLI command, wrapper command, or second approval workflow is introduced.
  </acceptance_criteria>
  <verify>
    <automated>make -f Makefile.cbm test</automated>
  </verify>
  <done>The pipeline can be driven deterministically into sequential or parallel mode by environment override while preserving the current auto path as the default.</done>
</task>

<task type="auto">
  <name>task 2: cover forced-mode selection with native parity regressions</name>
  <read_first>
    - codemap.md
    - src/pipeline/codemap.md
    - tests/test_parallel.c
    - src/pipeline/pipeline.c
    - src/pipeline/pipeline_internal.h
    - .planning/phases/03-real-repo-semantic-verification/03-CONTEXT.md
  </read_first>
  <files>tests/test_parallel.c</files>
  <action>Add native tests in `tests/test_parallel.c` for the new mode-selection contract instead of pushing this proof-layer concern into the shell harness. Create focused tests named with the substring `forced_pipeline_mode` that validate: (1) auto keeps the current threshold rule, (2) forced sequential selects sequential even when the repo would otherwise qualify for parallel, and (3) forced parallel selects parallel for the GDScript parity fixture without requiring byte-for-byte graph equality. Reuse the existing `suite_parallel` organization and temporary-fixture style already present in this file. Keep these tests about native selection/parity mechanics only; do not add proof-artifact assertions here, per the phase guidance.</action>
  <acceptance_criteria>
    - `tests/test_parallel.c` contains at least one `TEST(` name with the substring `forced_pipeline_mode`.
    - `tests/test_parallel.c` references `CBM_FORCE_PIPELINE_MODE` or the shared mode-selection helper.
    - `tests/test_parallel.c` keeps the existing GDScript parity tests and adds new tests rather than replacing them.
    - `tests/test_parallel.c` still registers the new tests inside `SUITE(parallel)`.
  </acceptance_criteria>
  <verify>
    <automated>make -f Makefile.cbm test</automated>
  </verify>
  <done>Native tests prove the proof harness can rely on deterministic sequential and parallel selection for the same target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof harness → pipeline env | Untrusted environment values choose the indexing mode |
| pipeline selection → proof evidence | Incorrect selection or silent fallback would misstate SEM-06 parity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `src/pipeline/pipeline.c` env override | mitigate | Accept only `auto`, `sequential`, or `parallel`; reject or warn on any other value and preserve `auto` behavior. |
| T-03-02 | Repudiation | pipeline mode logs | mitigate | Emit explicit `pipeline.mode_selection` fields for requested and selected mode so proof artifacts can prove which path ran. |
| T-03-03 | Denial of Service | forced parallel on unsupported host | mitigate | Fail deterministically when `parallel` is forced but `worker_count <= 1` instead of spinning or silently changing mode. |
|
</threat_model>

<verification>
- `make -f Makefile.cbm test` passes.
- `grep -n "CBM_FORCE_PIPELINE_MODE\|pipeline.mode_selection" src/pipeline/pipeline.c src/pipeline/pipeline_internal.h tests/test_parallel.c` shows the new contract in code and tests.
</verification>

<success_criteria>
- Deterministic mode forcing exists for the same repo target.
- Native regressions cover the mode-selection contract.
- The canonical proof harness has a stable product-level mechanism it can drive in later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-repo-semantic-verification/03-real-repo-semantic-verification-01-SUMMARY.md`
</output>
