// Vendored tree-sitter grammar: vb_dotnet (VB.NET)
// Each grammar compiled as separate unit (conflicting static symbols).
// No external scanner: this grammar is parser.c only.
#include "vendored/grammars/vb_dotnet/parser.c"
